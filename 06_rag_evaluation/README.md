# 第五步：RAG效果评估 📊

## 模块概述

本模块介绍如何系统地评估和优化RAG系统的效果。评估是持续改进的基础，好的评估体系能够指导我们找到系统的瓶颈和优化方向。

## 学习目标

- ✅ 理解RAG评估的关键指标
- ✅ 掌握手动评估和自动评估方法
- ✅ 学会使用评估工具和框架
- ✅ 建立持续优化的评估流程

## RAG评估的三个层次

```
┌─────────────────────────────────────┐
│  1. 检索评估（Retrieval）           │
│  能否检索到正确的文档？              │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│  2. 生成评估（Generation）          │
│  答案是否准确、完整、相关？          │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│  3. 端到端评估（End-to-End）        │
│  整体用户体验如何？                  │
└─────────────────────────────────────┘
```

## 一、检索评估指标

### 核心指标

| 指标 | 定义 | 计算公式 | 理想值 |
|------|------|----------|--------|
| **Precision@K** | 前K个结果中正确的比例 | 正确文档数 / K | 越高越好 |
| **Recall@K** | 前K个结果覆盖了多少正确文档 | 检索到的正确文档数 / 总正确文档数 | 越高越好 |
| **MRR** | 第一个正确结果的排名倒数 | 1 / 第一个正确结果的位置 | 越高越好 |
| **NDCG@K** | 归一化折损累计增益 | 考虑排序质量的综合指标 | 越高越好 |

### 评估示例

```python
def evaluate_retrieval(query, retrieved_docs, ground_truth_docs, k=5):
    """
    评估检索效果
    
    Args:
        query: 查询问题
        retrieved_docs: 检索到的文档列表
        ground_truth_docs: 真实相关文档列表
        k: 评估前k个结果
    """
    # 取前k个结果
    top_k_docs = retrieved_docs[:k]
    
    # Precision@K
    relevant_retrieved = [doc for doc in top_k_docs if doc in ground_truth_docs]
    precision_at_k = len(relevant_retrieved) / k
    
    # Recall@K
    recall_at_k = len(relevant_retrieved) / len(ground_truth_docs)
    
    # MRR
    for i, doc in enumerate(top_k_docs, 1):
        if doc in ground_truth_docs:
            mrr = 1 / i
            break
    else:
        mrr = 0
    
    return {
        "precision@k": precision_at_k,
        "recall@k": recall_at_k,
        "mrr": mrr
    }
```

### 检索评估流程

```python
# 1. 准备测试集
test_queries = [
    {
        "query": "什么是RAG？",
        "relevant_docs": ["doc_1", "doc_3", "doc_7"]
    },
    # ... 更多测试用例
]

# 2. 批量评估
results = []
for test_case in test_queries:
    retrieved = retriever.get_relevant_documents(test_case["query"])
    metrics = evaluate_retrieval(
        test_case["query"],
        retrieved,
        test_case["relevant_docs"]
    )
    results.append(metrics)

# 3. 计算平均指标
avg_precision = np.mean([r["precision@k"] for r in results])
avg_recall = np.mean([r["recall@k"] for r in results])
print(f"平均Precision@5: {avg_precision:.3f}")
print(f"平均Recall@5: {avg_recall:.3f}")
```

## 二、生成评估指标

### 答案质量维度

| 维度 | 说明 | 评估方法 |
|------|------|----------|
| **准确性** | 答案是否正确 | 人工标注 / LLM评分 |
| **完整性** | 是否回答了所有方面 | 要点覆盖率 |
| **相关性** | 是否切题 | 语义相似度 |
| **忠实性** | 是否基于参考资料 | 事实一致性检查 |
| **流畅性** | 语言是否自然 | 困惑度 / 人工评分 |

### LLM-as-Judge 评估

使用强大的LLM（如GPT-4）评估答案质量：

```python
def llm_judge_answer(question, answer, reference_docs):
    """使用LLM评估答案质量"""
    
    judge_prompt = f"""
    你是一个严格的评审专家，请评估以下答案的质量。

    【问题】
    {question}

    【参考资料】
    {reference_docs}

    【生成的答案】
    {answer}

    请从以下维度评分（1-5分）：

    1. 准确性（Accuracy）：答案是否正确
       - 5分：完全准确
       - 3分：基本准确但有小错误
       - 1分：错误或误导

    2. 完整性（Completeness）：是否全面回答
       - 5分：回答了所有要点
       - 3分：回答了主要要点
       - 1分：遗漏重要信息

    3. 忠实性（Faithfulness）：是否基于参考资料
       - 5分：完全基于参考资料
       - 3分：大部分基于，有少量推测
       - 1分：包含参考资料外的内容

    4. 相关性（Relevance）：是否切题
       - 5分：完全切题
       - 3分：基本相关但有偏离
       - 1分：答非所问

    请按以下格式输出：
    准确性：[分数] - [理由]
    完整性：[分数] - [理由]
    忠实性：[分数] - [理由]
    相关性：[分数] - [理由]
    总分：[平均分]
    """
    
    evaluation = llm.invoke(judge_prompt)
    return parse_evaluation(evaluation)
```

### RAGAS评估框架

[RAGAS](https://github.com/explodinggradients/ragas) 是专门为RAG设计的评估框架：

```python
from ragas import evaluate
from ragas.metrics import (
    faithfulness,         # 忠实性
    answer_relevancy,     # 答案相关性
    context_relevancy,    # 上下文相关性
    context_recall,       # 上下文召回率
)

# 准备评估数据
eval_data = {
    "question": ["什么是RAG？", ...],
    "answer": ["RAG是...", ...],
    "contexts": [[doc1, doc2], ...],
    "ground_truths": [["标准答案1"], ...]
}

# 运行评估
results = evaluate(
    dataset=eval_data,
    metrics=[
        faithfulness,
        answer_relevancy,
        context_relevancy,
        context_recall,
    ]
)

print(results)
```

## 三、端到端评估

### 用户体验指标

| 指标 | 说明 | 获取方式 |
|------|------|----------|
| **响应时间** | 从提问到得到答案的时间 | 系统日志 |
| **用户满意度** | 用户对答案的满意程度 | 👍👎反馈 |
| **问题解决率** | 一次性解决问题的比例 | 是否继续追问 |
| **会话轮数** | 平均对话轮数 | 对话日志 |

### A/B测试

对比不同RAG策略的效果：

```python
# 版本A：基础向量检索
retriever_a = VectorRetriever(embedding_model="text-embedding-3-small")

# 版本B：向量+Rerank
retriever_b = VectorRetriever(embedding_model="text-embedding-3-small")
retriever_b = add_reranker(retriever_b)

# 随机分配用户
def get_retriever(user_id):
    if hash(user_id) % 2 == 0:
        return retriever_a, "A"
    else:
        return retriever_b, "B"

# 收集指标
metrics_a = []
metrics_b = []

# 分析结果
print(f"版本A 平均满意度: {np.mean(metrics_a)}")
print(f"版本B 平均满意度: {np.mean(metrics_b)}")
```

## 四、评估数据集构建

### 方法1：人工标注

```python
# 标注格式
test_cases = [
    {
        "query": "公司的年假政策是什么？",
        "relevant_docs": ["doc_123", "doc_456"],  # 相关文档ID
        "ideal_answer": "根据公司政策，员工入职满一年享有5天年假...",  # 理想答案
        "must_include": ["5天", "入职满一年"],  # 必须包含的关键信息
        "must_not_include": ["病假", "事假"]  # 不应包含的内容
    }
]
```

### 方法2：从日志中提取

```python
# 从用户反馈中筛选好坏案例
good_cases = filter_logs(
    feedback="positive",
    min_confidence=0.8
)

bad_cases = filter_logs(
    feedback="negative"
)

# 分析差异，找到优化方向
```

### 方法3：合成数据

```python
# 使用LLM生成测试问题
def generate_test_questions(document):
    prompt = f"""
    基于以下文档，生成5个不同难度的测试问题：

    文档：
    {document}

    要求：
    1. 包含简单事实性问题
    2. 包含需要推理的问题
    3. 包含需要综合多处信息的问题

    格式：
    问题1：[问题内容]
    答案1：[参考答案]
    """
    
    return llm.invoke(prompt)
```

## 五、持续优化流程

```
┌──────────────┐
│ 1. 收集数据  │ → 用户查询日志、反馈
└──────────────┘
       ↓
┌──────────────┐
│ 2. 运行评估  │ → 批量评估现有系统
└──────────────┘
       ↓
┌──────────────┐
│ 3. 分析瓶颈  │ → 检索差？生成差？
└──────────────┘
       ↓
┌──────────────┐
│ 4. 针对优化  │ → 调整Prompt、模型、参数
└──────────────┘
       ↓
┌──────────────┐
│ 5. A/B测试   │ → 对比新旧版本
└──────────────┘
       ↓
┌──────────────┐
│ 6. 上线迭代  │ → 持续改进
└──────────────┘
```

## 六、评估工具推荐

### 1. RAGAS
- 专为RAG设计
- 提供多种评估指标
- 支持LangChain集成
- 项目地址：https://github.com/explodinggradients/ragas

### 2. TruLens
- 可视化评估面板
- 实时监控RAG性能
- 提供调试工具
- 项目地址：https://github.com/truera/trulens

### 3. Phoenix (by Arize AI)
- 开源评估平台
- LLM可观测性
- 支持追踪和分析
- 项目地址：https://github.com/Arize-ai/phoenix

### 4. LangSmith
- LangChain官方工具
- 完整的调试和评估功能
- 支持团队协作
- 网站：https://smith.langchain.com/

## 七、实战案例

### 案例：优化客服知识库

```python
# 初始评估
baseline_metrics = evaluate_system(test_set)
# 结果：准确率 65%，用户满意度 3.2/5

# 问题分析
# 1. 检索问题：Recall@5 只有 40%
# 2. 生成问题：答案不够友好，过于官方

# 优化1：改进检索（加入Rerank）
retriever = add_reranker(retriever)
new_metrics_1 = evaluate_system(test_set)
# 结果：准确率 75% ↑，Recall@5 65% ↑

# 优化2：改进Prompt（调整语气）
prompt = update_prompt_with_friendly_tone(prompt)
new_metrics_2 = evaluate_system(test_set)
# 结果：用户满意度 4.1/5 ↑

# A/B测试验证
ab_test_results = run_ab_test(baseline, optimized, days=7)
# 新版本获胜，全量上线
```

## 常见问题

### Q1: 评估数据集需要多大？
**A**: 
- 小规模：50-100条（快速验证）
- 中等规模：500-1000条（可靠评估）
- 大规模：5000+条（充分评估）

### Q2: 人工评估 vs 自动评估？
**A**: 
- 开发阶段：自动评估为主（快速迭代）
- 上线前：人工评估关键案例
- 持续监控：自动评估 + 采样人工审核

### Q3: 如何处理主观性问题？
**A**: 
- 制定明确的评分标准
- 多人标注取平均
- 使用LLM辅助评估

## 实践练习

### 📓 rag_evaluation.ipynb（开发中）

计划包含：

1. **检索评估实战**
   - 构建测试集
   - 计算各项指标
   - 可视化结果

2. **生成评估实战**
   - LLM-as-Judge实现
   - RAGAS框架使用

3. **A/B测试实战**
   - 对比不同策略
   - 统计显著性检验

4. **优化案例**
   - 真实问题分析
   - 迭代优化过程

## 下一步

🎉 恭喜完成RAG系统的完整学习！接下来可以：

➡️ 探索 [未来技术方向](../future_practice/Readme.md)
➡️ 阅读 [相关论文](../paper_learning/)
➡️ 部署自己的RAG应用

---

💡 **提示**：评估是持续改进的基础。建议建立评估数据集，每次优化都对比指标变化，确保改进有效！

